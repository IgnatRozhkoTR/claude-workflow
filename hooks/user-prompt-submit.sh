#!/bin/bash
# UserPromptSubmit hook: governed workflow reminder on every message.
cat << 'HINT'
You are the orchestrator. You coordinate, sub-agents execute. Never edit files directly. Use workspace MCP tools. Run /governed-workflow if unsure.
HINT
exit 0
